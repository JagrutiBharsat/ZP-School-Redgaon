# Server Configuration
PORT=5000
NODE_ENV=production

# Database
MONGO_URI=your_mongodb_connection_string_here

# JWT Secret (use a strong random string)
JWT_SECRET=your_jwt_secret_key_here

# Note: CORS is now configured to accept multiple origins in server.js
# Allowed origins: localhost, Vercel URL, and custom domain
